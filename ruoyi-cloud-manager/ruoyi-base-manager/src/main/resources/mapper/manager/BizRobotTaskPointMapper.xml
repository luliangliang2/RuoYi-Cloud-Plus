<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.manager.mapper.BizRobotTaskPointMapper">

    <select id="selectByTaskId" resultType="org.dromara.manager.domain.vo.BizRobotTaskPointVo">
        select task_point_id,
               task_id,
               task_no,
               loop_no,
               route_id,
               point_id,
               point_name,
               point_seq,
               required_flag,
               gcj02_lng,
               gcj02_lat,
               bd09_lng,
               bd09_lat,
               wgs84_lng,
               wgs84_lat,
               point_status,
               arrive_time,
               finish_time,
               report_payload,
               remark
        from biz_robot_task_point
        where del_flag = '0'
          and task_id = #{taskId}
        order by loop_no asc, point_seq asc, task_point_id asc
    </select>

</mapper>
