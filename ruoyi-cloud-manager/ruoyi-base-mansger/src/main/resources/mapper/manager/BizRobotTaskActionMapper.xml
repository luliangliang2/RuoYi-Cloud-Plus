<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.manager.mapper.BizRobotTaskActionMapper">

    <select id="selectByTaskId" resultType="org.dromara.manager.domain.vo.BizRobotTaskActionVo">
        select task_action_id,
               task_id,
               task_no,
               task_point_id,
               loop_no,
               point_id,
               point_seq,
               action_id,
               action_code,
               action_name,
               action_type,
               action_seq,
               action_params,
               action_status,
               start_time,
               finish_time,
               report_payload,
               error_message,
               remark
        from biz_robot_task_action
        where del_flag = '0'
          and task_id = #{taskId}
        order by loop_no asc, point_seq asc, action_seq asc, task_action_id asc
    </select>

</mapper>
