<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.manager.mapper.BizTaskTemplateMapper">

    <select id="selectTaskTemplateList" resultType="org.dromara.manager.domain.vo.BizTaskTemplateVo">
        select t.template_id,
               t.template_code,
               t.template_name,
               t.route_id,
               r.route_name,
               t.template_desc,
               t.status,
               t.create_time,
               t.remark
        from biz_task_template t
            left join biz_scene_route r on r.route_id = t.route_id and r.del_flag = '0'
        <where>
            t.del_flag = '0'
            <if test="bo.templateCode != null and bo.templateCode != ''">
                and t.template_code like concat('%', #{bo.templateCode}, '%')
            </if>
            <if test="bo.templateName != null and bo.templateName != ''">
                and t.template_name like concat('%', #{bo.templateName}, '%')
            </if>
            <if test="bo.routeId != null">
                and t.route_id = #{bo.routeId}
            </if>
            <if test="bo.status != null and bo.status != ''">
                and t.status = #{bo.status}
            </if>
        </where>
        order by t.create_time desc, t.template_id desc
    </select>

    <select id="selectTaskTemplatePage" resultType="org.dromara.manager.domain.vo.BizTaskTemplateVo">
        select t.template_id,
               t.template_code,
               t.template_name,
               t.route_id,
               r.route_name,
               t.template_desc,
               t.status,
               t.create_time,
               t.remark
        from biz_task_template t
            left join biz_scene_route r on r.route_id = t.route_id and r.del_flag = '0'
        <where>
            t.del_flag = '0'
            <if test="bo.templateCode != null and bo.templateCode != ''">
                and t.template_code like concat('%', #{bo.templateCode}, '%')
            </if>
            <if test="bo.templateName != null and bo.templateName != ''">
                and t.template_name like concat('%', #{bo.templateName}, '%')
            </if>
            <if test="bo.routeId != null">
                and t.route_id = #{bo.routeId}
            </if>
            <if test="bo.status != null and bo.status != ''">
                and t.status = #{bo.status}
            </if>
        </where>
        order by t.create_time desc, t.template_id desc
    </select>

</mapper>
