# 将此文件夹下所有配置文件导入到 `grafana` 内生成展示看板

- `Spring Boot 3.x Statistics.json`: 基于 Grafana dashboard `20727`，用于 Spring Boot 3.x/4.x Micrometer Prometheus 指标。
- `SLS JVM监控大盘.json`: 基于 Grafana dashboard `4701`，用于 JVM Micrometer 指标。
- `Nacos.json`: 基于 Grafana dashboard `13221`，用于 Nacos Prometheus 指标。
